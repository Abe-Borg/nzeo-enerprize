from django.apps import AppConfig


class MapsWebpagesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'maps_webpages'
    verbose_name = 'Maps Webpages represents the homes pages for different school sites or even specific buildings within a school site'
