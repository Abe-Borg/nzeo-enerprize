from django.apps import AppConfig


# class MapsLocationsConfig(AppConfig):
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'maps_locations'
#     verbose_name = 'Maps locations app manages the homes pages and other information for different locations on map.'
